# infra

Deployment and infrastructure configuration that doesn't belong to a single domain: e.g. cloud
resources, reverse proxy config, environment-specific settings, or deployment scripts for
`backend/` and `frontend/`.

Empty for now. There's no containerized/deployed workflow yet — this is a placeholder for once
the project is far enough along that "how do we run this for real" becomes a real question
(local dev in the meantime just means running `backend/` and `frontend/` directly).
