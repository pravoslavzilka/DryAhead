# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# --- Node / frontend ---
node_modules/
dist/
.vite/

# --- PlatformIO ---
.pio/
.pioenvs/
.piolibdeps/

# --- OS / editor junk ---
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# --- Claude Code ---
# Shared config (settings.json, hooks/, skills/, agents/) is committed so
# cloud/unattended sessions see it too. Only personal local overrides are
# ignored.
.claude/settings.local.json
# Security-sensitive suggestions (specific unrotated-credential locations,
# etc.) - this repo is public, don't index that in git history.
.claude/SUGGESTIONS.local.md
